#Change Content
- name: Change content 
  hosts: all
  become: yes
  become_user: root
  tasks:
    - name: copy content
      replace:
       path: /var/www/cgi-bin/mycmd.cgi
       regexp: "IP Address"
       replace: "Address"
    - name: copy content
      replace:
       path: /var/www/cgi-bin/mycmd.cgi
       regexp: "ip addr" 
       replace: "ifconfig"
