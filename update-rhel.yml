---
- name: Update system
  hosts: all
  become: yes
  tasks:
    - name: Upgrade all packages
      ansible.builtin.dnf:
        name: "*"
        state: latest
        update_only: yes
        notify: result
    - name: Show Output
      when: result|succeeded
      debug: msg="{{ result.stdout_lines }}"
